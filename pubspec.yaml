name: flutter_error_handling
description: A new Flutter project.

publish_to: "none"

version: 1.1.0+1

environment:
  sdk: ">=3.0.1 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  dio:
  code_coverage: ^1.4.0

  cupertino_icons: ^1.0.2
  firebase_core: ^2.13.1
  firebase_crashlytics: ^3.3.2
  equatable: ^2.0.5
  provider: ^6.0.5
  error_handler:
    path: ./error_handler

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0
  mocktail:
  faker:

flutter:
  uses-material-design: true
  assets:
    - assets/images/wallpaper.jpg
